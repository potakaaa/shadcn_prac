import { Button } from "./components/ui/button";

const App = () => {
  return (
    <div className="flex size-full bg-zinc-800 items-center justify-center h-screen">
      <Button className="flex w-32 h-14 rounded-full">Click me</Button>
    </div>
  );
};

export default App;
